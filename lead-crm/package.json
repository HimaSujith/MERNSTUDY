{
  "name": "lead-crm",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "seed:admin": "npm run seed:admin -w server",
    "test:server": "npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
